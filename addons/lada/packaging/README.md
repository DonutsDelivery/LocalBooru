# Bundle packaging

`build-bundles.sh` consumes the pinned upstream LADA source and lock file to produce Linux x86_64 common, accelerator, model, and Corresponding Source archives. It is intended for a clean release builder; generated payloads are never included in the LocalBooru base artifact.
